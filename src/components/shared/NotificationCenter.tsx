import React from "react";
import styled from "styled-components";
import { iSystemMessage } from "../../constants/Types";

type iNotificationCenterProps = {
  messages: iSystemMessage[];
};

export default function NotificationCenter(props: iNotificationCenterProps) {
  return <React.Fragment />;
}
